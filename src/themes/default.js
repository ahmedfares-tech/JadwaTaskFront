// import { createTheme } from '@material-ui/core/styles';
// export default createTheme((props) =>return {
//     palette: {
//         mode: "dark"
//     }
// });
export default function Default({ darkMode }) {
    return {
        palette: {
            // mode: darkMode ? "dark" : "light",
            mode:'light'
        }
    }
}
// export default function DefaultTheme(props) {
//     return theme;

// }

